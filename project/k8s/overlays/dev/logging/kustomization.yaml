apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/logging

namespace: logging

labels:
  - pairs:
      app.kubernetes.io/environment: dev
    includeSelectors: false
    includeTemplates: true

patches:
  - path: patches/es-small.yaml
  - path: patches/kibana-small.yaml
